apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db21c-oracle-db
  namespace: oracle
  labels:
    app: db21c-oracle-db
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: "30Gi"
  storageClassName: "nfs-client"